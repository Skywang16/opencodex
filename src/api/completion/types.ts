/**
 * Completion API types — re-exported from the single source of truth.
 */
export type {
  CompletionEngineStatus,
  CompletionItem,
  CompletionRequest,
  CompletionResponse,
  CompletionResult,
  CompletionRetryOptions,
  CompletionStats,
} from '@/types/domain/completion'
