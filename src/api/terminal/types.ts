/**
 * Terminal API types — re-exported from the single source of truth.
 */
export type {
  BatchTerminalResize,
  CommandEventPayload,
  CommandStatus,
  CreateTerminalWithShellOptions,
  CursorConfig,
  ProcessReplayEvent,
  ReplayEvent,
  ShellConfig,
  ShellInfo,
  TerminalBehaviorConfig,
  TerminalConfig,
  TerminalConfigValidationResult,
  TerminalCreateOptions,
  TerminalExitEvent,
  TerminalOperationResult,
  TerminalResizeEvent,
  TerminalResizeOptions,
  TerminalRetryOptions,
  TerminalStats,
  TerminalWriteOptions,
} from '@/types/domain/terminal'
